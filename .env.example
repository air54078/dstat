# Cloudflare Analytics API token (read-only, never commit the real value)
CLOUDFLARE_API_TOKEN=
# Zone ID that contains the hostname you want to monitor
CLOUDFLARE_ZONE_ID=
# Hostname to filter in Cloudflare HTTP Analytics
CLOUDFLARE_HOSTNAME=dstat.kdns.fr
